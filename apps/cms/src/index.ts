// Strapi 앱 진입점
// 서버 라이프사이클 훅을 등록할 수 있습니다.

export default {
  /**
   * 앱이 시작되기 전에 실행되는 함수
   * 커스텀 로직, 미들웨어, 이벤트 리스너 등을 등록합니다.
   */
  register({ strapi }) {
    // 커스텀 등록 로직
    // 예: 커스텀 GraphQL 확장, 미들웨어 등록 등
  },

  /**
   * 앱이 완전히 부팅된 후 실행되는 함수
   * 데이터베이스 연결이 완료된 상태에서 초기 데이터 설정 등을 수행합니다.
   */
  bootstrap({ strapi }) {
    // 부트스트랩 로직은 src/bootstrap.ts에서 관리합니다.
  },

  /**
   * 앱이 종료되기 전에 실행되는 함수
   * 리소스 정리, 연결 해제 등을 수행합니다.
   */
  destroy({ strapi }) {
    // 정리(cleanup) 로직
  },
};
